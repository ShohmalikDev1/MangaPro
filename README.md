# MangaPro

Scaffold for MangaPro project (Next.js frontend + Express backend + Prisma/Postgres).

See `backend/prisma/schema.prisma` for the database schema.

Environment variables should be set according to project spec.
